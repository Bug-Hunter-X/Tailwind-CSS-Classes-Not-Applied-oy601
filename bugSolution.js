```javascript
// Ensure Tailwind directives are correctly configured in your main CSS file or in the `tailwind.config.js` file.
// Make sure your build process correctly incorporates Tailwind CSS (PostCSS, Webpack).
// Check for CSS conflicts that might overwrite your Tailwind classes.

<div class="bg-red-500 p-4">
  <p>This text should now be red and have padding.</p>
</div>
```